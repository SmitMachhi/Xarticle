--------------------------------------------------------------------------------
title: "Xarticle.co"
description: "Web app with a minimal stateless extract backend for public X/Twitter links and clean PDF or Markdown exports."
last_updated: "2026-02-27"
source: "https://xarticle.co"
--------------------------------------------------------------------------------

# Xarticle.co

Xarticle.co is a browser-based tool that turns a public X/Twitter status or long-form article URL into a readable export.

## Product Summary

- Website: https://xarticle.co
- Type: Web application + stateless extraction endpoint
- Main use case: Export public X content into portable formats for reading or LLM workflows

## Supported Inputs

- Public status URLs, for example: `https://x.com/<handle>/status/<id>`
- Public article URLs, for example: `https://x.com/i/articles/<id>`

## Unsupported Inputs

- Private or locked accounts
- Non-X URLs
- Invalid or malformed URLs

## Extraction Flow

Xarticle.co uses a single backend endpoint (`/api/extract`) with:

1. self-hosted status/thread retrieval for status URLs
2. Direct HTML fetch for long-form article URLs

## Exports

- PDF export
  - Paper size: `A4` or `Letter`
  - Margin: `Default` or `Minimum`
- Markdown export
  - If media is present, output is a ZIP bundle with:
    - `article.md`
    - `assets/` folder with downloaded images (when available)

## Data and Privacy

- No login required
- No account required
- Extraction is performed by a stateless backend endpoint
- File generation runs in the browser session
- Designed for public page processing only

## Operational Notes

- X page structure can change over time and may affect parsing reliability
- Some metrics may be unavailable on certain pages
- Status/thread reliability depends on upstream payload availability

## Contact

- https://x.com/thesmitmachhi
