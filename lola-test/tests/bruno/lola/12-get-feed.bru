meta {
  name: 12 Get Feed (task_completed event)
  type: http
  seq: 16
}

get {
  url: {{BASE_URL}}/feed
  auth: bearer
}

auth:bearer {
  token: {{JWT_1}}
}

assert {
  res.status: eq 200
  res.body.events: isDefined
}
