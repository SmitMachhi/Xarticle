meta {
  name: 10 Complete Task (XP + ring + feed)
  type: http
  seq: 14
}

post {
  url: {{BASE_URL}}/tasks/{{TASK_ID}}/complete
  body: json
  auth: bearer
}

auth:bearer {
  token: {{JWT_1}}
}

body:json {
  {}
}

assert {
  res.status: eq 200
  res.body.xp_awarded: isDefined
}
