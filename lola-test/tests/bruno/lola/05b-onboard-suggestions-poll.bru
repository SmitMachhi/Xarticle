meta {
  name: 05b Poll Onboard Suggestions
  type: http
  seq: 10
}

get {
  url: {{BASE_URL}}/onboard/suggestions/{{ONBOARD_JOB_ID}}
  auth: bearer
}

auth:bearer {
  token: {{JWT_1}}
}

assert {
  res.status: eq 200
  res.body.job_id: isDefined
  res.body.status: isDefined
}
