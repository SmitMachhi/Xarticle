meta {
  name: 15 Get Lola Messages (catchup pending)
  type: http
  seq: 20
}

get {
  url: {{BASE_URL}}/lola/messages
  auth: bearer
}

auth:bearer {
  token: {{JWT_1}}
}

assert {
  res.status: eq 200
  res.body.messages: isDefined
}
