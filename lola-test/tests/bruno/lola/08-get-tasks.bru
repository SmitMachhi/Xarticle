meta {
  name: 08 Get Tasks (sorted by next_due)
  type: http
  seq: 12
}

get {
  url: {{BASE_URL}}/tasks
  auth: bearer
}

auth:bearer {
  token: {{JWT_1}}
}

assert {
  res.status: eq 200
  res.body.tasks: isDefined
}
